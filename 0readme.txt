first, make a database, and the name should be "student_info".


after that ,,for run crud operation import "trys.sql" file on "student_info" database..


and if u want to run the image upload system, then import the "img_url.sql" file on the "student_info" database too. 